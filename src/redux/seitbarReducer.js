let initialState = {
    friendsOnline: [
        {id: 1, name: "Dima"},
        {id: 2, name: "Petia"},
        {id: 3, name: "Valera"}
      ]
}

const seitbarReducer = (state = initialState,action) => {
   
    return state;
}

export default seitbarReducer;